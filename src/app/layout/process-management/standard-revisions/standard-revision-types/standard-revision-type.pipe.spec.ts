import { StandardRevisionTypePipe } from './standard-revision-type.pipe';

describe('StandardRevisionTypePipe', () => {
  it('create an instance', () => {
    const pipe = new StandardRevisionTypePipe();
    expect(pipe).toBeTruthy();
  });
});
