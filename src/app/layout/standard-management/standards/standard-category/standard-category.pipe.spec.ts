import { StandardCategoryPipe } from './standard-category.pipe';

describe('StandardCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new StandardCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
