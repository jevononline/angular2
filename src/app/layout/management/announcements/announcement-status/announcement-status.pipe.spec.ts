import { AnnouncementStatusPipe } from './announcement-status.pipe';

describe('AnnouncementStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new AnnouncementStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
