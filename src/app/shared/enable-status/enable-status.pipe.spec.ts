import { EnableStatusPipe } from './enable-status.pipe';

describe('EnableStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new EnableStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
